import React from "react";

const AuthContext = React.createContext();

export default AuthContext;

/*
    1. Create a variable "AuthContext" and set it's value to: React.createContext();
    2. This will return an object, that you will use through the app pages

*/
